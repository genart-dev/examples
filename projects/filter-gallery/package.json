{
  "name": "filter-gallery",
  "version": "1.0.0",
  "description": "Before/after filter chains using plugin-filters — grain, duotone, chromatic aberration, vignette",
  "private": true,
  "scripts": {
    "render": "bash renders/render.sh"
  }
}
